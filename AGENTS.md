# AGENTS.md

After changing a plugin's content, set the `version` field in both `plugins/<name>/.claude-plugin/plugin.json` and `plugins/<name>/skills/<name>/package.json` to today's date as `YYYY.M.D` (no zero-padding).
